import { GoogelSignDirective } from './googel-sign.directive';

describe('GoogelSignDirective', () => {
  it('should create an instance', () => {
    const directive = new GoogelSignDirective();
    expect(directive).toBeTruthy();
  });
});
